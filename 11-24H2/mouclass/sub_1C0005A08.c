/*\n * XREFs of sub_1C0005A08 @ 0x1C0005A08\n * Callers:\n *     sub_1C0004AB0 @ 0x1C0004AB0 (sub_1C0004AB0.c)\n *     sub_1C000EB40 @ 0x1C000EB40 (sub_1C000EB40.c)\n *     sub_1C000EBE0 @ 0x1C000EBE0 (sub_1C000EBE0.c)\n * Callees:\n *     Tag @ 0x1C0005B50 (Tag.c)\n */\n\nNTSTATUS __fastcall sub_1C0005A08(__int64 a1, char a2)\n{\n  struct _IO_REMOVE_LOCK *v2; // rsi\n  NTSTATUS result; // eax\n  __int64 Pool2; // rbx\n  PIO_WORKITEM WorkItem; // rax\n\n  v2 = (struct _IO_REMOVE_LOCK *)(a1 + 32);\n  result = IoAcquireRemoveLockEx((PIO_REMOVE_LOCK)(a1 + 32), Tag, File, 1u, 0x20u);\n  if ( result >= 0 )\n  {\n    Pool2 = ExAllocatePool2(64LL, 32LL, 1131769677LL);\n    if ( Pool2 )\n    {\n      WorkItem = IoAllocateWorkItem(*(PDEVICE_OBJECT *)a1);\n      *(_QWORD *)(Pool2 + 16) = WorkItem;\n      if ( !WorkItem )\n      {\n        IoReleaseRemoveLockEx(v2, Tag, 0x20u);\n        ExFreePoolWithTag((PVOID)Pool2, 0);\n        return -1073741670;\n      }\n      *(_QWORD *)(Pool2 + 8) = a1;\n      *(_BYTE *)(Pool2 + 24) = a2;\n      if ( KeGetCurrentIrql() )\n        IoQueueWorkItem(*(PIO_WORKITEM *)(Pool2 + 16), Tag, DelayedWorkQueue, (PVOID)Pool2);\n      else\n        Tag(*(PDEVICE_OBJECT *)a1, (PVOID)Pool2);\n    }\n    else\n    {\n      IoReleaseRemoveLockEx(v2, Tag, 0x20u);\n    }\n    return 0;\n  }\n  return result;\n}\n