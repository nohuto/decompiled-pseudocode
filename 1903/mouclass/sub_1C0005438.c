/*\n * XREFs of sub_1C0005438 @ 0x1C0005438\n * Callers:\n *     sub_1C0005230 @ 0x1C0005230 (sub_1C0005230.c)\n *     sub_1C000E390 @ 0x1C000E390 (sub_1C000E390.c)\n *     sub_1C000E430 @ 0x1C000E430 (sub_1C000E430.c)\n * Callees:\n *     Tag @ 0x1C0005580 (Tag.c)\n */\n\nNTSTATUS __fastcall sub_1C0005438(__int64 a1, char a2)\n{\n  struct _IO_REMOVE_LOCK *v2; // rsi\n  NTSTATUS result; // eax\n  _QWORD *PoolWithTag; // rbx\n  struct _IO_WORKITEM *WorkItem; // rax\n\n  v2 = (struct _IO_REMOVE_LOCK *)(a1 + 32);\n  result = IoAcquireRemoveLockEx((PIO_REMOVE_LOCK)(a1 + 32), Tag, File, 1u, 0x20u);\n  if ( result >= 0 )\n  {\n    PoolWithTag = ExAllocatePoolWithTag((POOL_TYPE)512, 0x20uLL, 0x43756F4Du);\n    if ( PoolWithTag )\n    {\n      WorkItem = IoAllocateWorkItem(*(PDEVICE_OBJECT *)a1);\n      PoolWithTag[2] = WorkItem;\n      if ( !WorkItem )\n      {\n        IoReleaseRemoveLockEx(v2, Tag, 0x20u);\n        ExFreePoolWithTag(PoolWithTag, 0);\n        return -1073741670;\n      }\n      PoolWithTag[1] = a1;\n      *((_BYTE *)PoolWithTag + 24) = a2;\n      if ( KeGetCurrentIrql() )\n        IoQueueWorkItem(WorkItem, Tag, DelayedWorkQueue, PoolWithTag);\n      else\n        Tag(*(PDEVICE_OBJECT *)a1, PoolWithTag);\n    }\n    else\n    {\n      IoReleaseRemoveLockEx(v2, Tag, 0x20u);\n    }\n    return 0;\n  }\n  return result;\n}\n