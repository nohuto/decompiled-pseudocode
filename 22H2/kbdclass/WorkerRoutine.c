/*\n * XREFs of WorkerRoutine @ 0x1C0002A50\n * Callers:\n *     <none>\n * Callees:\n *     sub_1C0002AE0 @ 0x1C0002AE0 (sub_1C0002AE0.c)\n */\n\nvoid __fastcall WorkerRoutine(PDEVICE_OBJECT DeviceObject, PIO_WORKITEM *Context)\n{\n  IRP *v2; // rbx\n  struct _IO_REMOVE_LOCK *DeviceExtension; // rdi\n\n  v2 = (IRP *)Context[1];\n  DeviceExtension = (struct _IO_REMOVE_LOCK *)DeviceObject->DeviceExtension;\n  v2->IoStatus.Status = sub_1C0002AE0(DeviceObject, Context);\n  v2->IoStatus.Information = 0LL;\n  IofCompleteRequest(v2, 0);\n  IoFreeWorkItem(*Context);\n  ExFreePoolWithTag(Context, 0);\n  IoReleaseRemoveLockEx(DeviceExtension + 1, WorkerRoutine, 0x20u);\n}\n